On window focus once DB reconnected...


  // -> check for DB connection

    // if DB connected

      // check for network connection
        // if no network add warning to navbar

      // -> check dropbox for more recent data
        
        // if more recent data
          // get data and update local state / re-render

